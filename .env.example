# Copy this file to `.env` to enable the phone number on the resume and
# cover letter pages. Leave unset (or empty) to keep it hidden — the
# default for the deployed site.
#
# Format is free-form; the `tel:` link strips non-digits and prepends +1.
NUXT_PUBLIC_PHONE=
